# Schedule API endpoint (host + path; app appends ?year= at runtime).
# In Cloudflare, set per-environment: prod host for Production, dev host for Preview.
NEXT_PUBLIC_SCHEDULE_ENDPOINT=https://api.schedule.myzenithstudy.com/schedule
