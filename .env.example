# Schedule API base origin (no path; app appends /schedule?year= at runtime).
# In Cloudflare, set per-environment: prod host for Production, dev host for Preview.
NEXT_PUBLIC_SCHEDULE_API_BASE_URL=https://api.schedule.myzenithstudy.com
